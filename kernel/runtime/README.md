# Kernel Runtime

Runtime define o ciclo de vida de uma execucao.

Esta area deve conter, futuramente, contratos de task, result, lease, cancellation, timeout e retry.

O runtime nao deve depender de modelo especifico.
