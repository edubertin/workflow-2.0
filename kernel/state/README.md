# Kernel State

Estado representa o progresso operacional do runtime.

Ele inclui tarefas, execucoes, artefatos, locks, checkpoints e resultados intermediarios.

Estado nao e memoria semantica; memoria vive em `kernel/memory`.
